name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

env:
  GO_VERSION: "1.25.12"

jobs:
  lint:
    name: Lint (Go)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: |
            apps/api/go.sum
            packages/go-common/go.sum

      - name: golangci-lint (apps/api)
        uses: golangci/golangci-lint-action@v8
        with:
          version: latest
          working-directory: apps/api

      - name: golangci-lint (packages/go-common)
        uses: golangci/golangci-lint-action@v8
        with:
          version: latest
          working-directory: packages/go-common

  test:
    name: Test (Go)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: |
            apps/api/go.sum
            packages/go-common/go.sum

      - name: Download dependencies (apps/api)
        working-directory: apps/api
        run: go mod download

      - name: Vet (apps/api)
        working-directory: apps/api
        run: go vet ./...

      - name: Run tests (apps/api, race + coverage)
        working-directory: apps/api
        run: go test -race -count=1 -covermode=atomic -coverprofile=coverage.out ./...

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: apps/api/coverage.out
          retention-days: 7

      - name: Vet (packages/go-common)
        working-directory: packages/go-common
        run: go vet ./...

      - name: Run tests (packages/go-common, race)
        working-directory: packages/go-common
        run: go test -race -count=1 ./...

  vuln:
    name: Vulnerability Scan (Go)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: |
            apps/api/go.sum
            packages/go-common/go.sum

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck (apps/api)
        working-directory: apps/api
        run: govulncheck ./...

      - name: Run govulncheck (packages/go-common)
        working-directory: packages/go-common
        run: govulncheck ./...

  build:
    name: Build (Go)
    runs-on: ubuntu-latest
    needs: [lint, test, vuln]
    defaults:
      run:
        working-directory: apps/api
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: apps/api/go.sum

      - name: Build binaries
        run: |
          go build -ldflags="-s -w" -o bin/server ./cmd/server
          go build -ldflags="-s -w" -o bin/migrate ./cmd/migrate
          go build -ldflags="-s -w" -o bin/worker ./cmd/worker

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: apps/api/bin/
          retention-days: 7

  frontend:
    name: Frontend (TS workspaces)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build api-client (types consumed by web + ai)
        run: bun run build:client

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun run test:ts

      - name: Build web (browser bundle)
        run: bun run build:web

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [build]
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Push only on branch pushes to main/master; on PRs, build to validate the
      # Dockerfile without publishing.
      - name: Login to GitHub Container Registry
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          # Repo root, not ./apps/api: apps/api/go.mod has a local `replace`
          # onto ../../packages/go-common, which must be inside the build
          # context for the Dockerfile to COPY it (see apps/api/Dockerfile).
          context: .
          file: apps/api/Dockerfile
          push: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
